# --- SECRETS (Most Important) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- DEPENDENCIES ---
node_modules/
jspm_packages/
# We usually keep package-lock.json to ensure everyone has the same versions
# package-lock.json 

# --- BUILD & OUTPUT ---
# These are the files created when the app is ready for the college to use
dist/
build/
out/
.next/

# --- LOGS & DEBUG ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# --- OS JUNK ---
.DS_Store
Thumbs.db
